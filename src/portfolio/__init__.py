"""Portfolio-level aggregation across instruments.

Combines independently-run, single-symbol backtest results into
portfolio-level equity, correlation, concentration, drawdown, and exposure.
See src/portfolio/aggregation.py's module docstring for the scope boundary
(post-hoc aggregation, not one simultaneous multi-instrument strategy).
"""
