# Bybit linear instrument-info snapshot for the initial v2 universe.
instrument_snapshot:
  source_url: https://api.bybit.com/v5/market/instruments-info?category=linear
  retrieved_at_utc: "2026-08-24T00:00:00Z"
quote_currency: USDT
settlement_currency: USDT
fees:
  maker_fee: 0.0002
  taker_fee: 0.00055
default_leverage: 10
symbols:
  BTCUSDT: {base_currency: BTC, price_precision: 1, size_precision: 3, price_increment: 0.1, size_increment: 0.001, contract_multiplier: 1}
  ETHUSDT: {base_currency: ETH, price_precision: 2, size_precision: 2, price_increment: 0.01, size_increment: 0.01, contract_multiplier: 1}
  SOLUSDT: {base_currency: SOL, price_precision: 2, size_precision: 1, price_increment: 0.01, size_increment: 0.1, contract_multiplier: 1}
